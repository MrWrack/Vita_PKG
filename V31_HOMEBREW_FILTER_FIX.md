# v3.1 Homebrew Filter Fix
- Search > Homebrew now scans installed homebrew only (`ux0:/app`).
- It no longer detects loose `.vpk` or `.pkg` files.
- `.vpk` files remain exclusively in `VPK Files / Convert`.
- `.pkg` files remain exclusively in `PKG Files / Install`.
- Search > Games remains separate.
