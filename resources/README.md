# head.bin resource

A valid homebrew package `head.bin` template is required by the Vita promotion
pipeline. Do not replace this with an empty file.

v1.1 intentionally does not ship a fabricated template. The next implementation
step is to incorporate a GPL-compatible VitaShell-derived template together with
the metadata/HMAC patching code and license attribution.
